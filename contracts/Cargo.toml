[package]
name = "contract"
version = "0.1.0"
edition = "2024"

[dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
overflow-checks = true
